# Milestone 60: Consistent non-chat page heroes

- [ ] **m60-01** — Add a shared branded page hero and migrate every non-chat
  screen, including setup/gates, while preserving each title and subtitle.
  Verification: `npm run lint --workspace web && npm run build --workspace web`.
- [ ] **m60-02** — Run the full project verification gate and archive the
  milestone. Verification: `npm run lint && npm test && npm run build`.
