# Milestone 57: Table copy spacing

- [ ] **m57-01** — Move the table copy control out of the table overlay into a
  separate action row with clear spacing above the table in inline and modal
  views. Verification: `npm run lint --workspace web && npm run build --workspace web`.
- [ ] **m57-02** — Run the full project verification gate and archive the
  milestone. Verification: `npm run lint && npm test && npm run build`.
