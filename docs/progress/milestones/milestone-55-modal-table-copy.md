# Milestone 55: Table copy in preview modal

- [ ] **m55-01** — Reuse the rich/TSV table copy control in the expanded table
  modal without reopening the modal when its table is clicked. Verification:
  `npm run lint --workspace web && npm run build --workspace web`.
- [ ] **m55-02** — Run the full project verification gate and archive the
  milestone. Verification: `npm run lint && npm test && npm run build`.
