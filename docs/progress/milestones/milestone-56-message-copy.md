# Milestone 56: Copy raw assistant responses

- [ ] **m56-01** — Add a Copy action below each non-empty assistant message
  that copies its original raw Markdown and briefly confirms success.
  Verification: `npm run lint --workspace web && npm run build --workspace web`.
- [ ] **m56-02** — Run the full project verification gate and archive the
  milestone. Verification: `npm run lint && npm test && npm run build`.
