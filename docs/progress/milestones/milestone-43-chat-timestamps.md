# Milestone 43: Browser-local chat timestamps

## Tasks

- [x] m43-01: Preserve event timestamps through transcript folding, including a separate timestamp for every reasoning iteration.
  - Verify: `cd app && npm test --workspace web`
- [ ] m43-02: Render browser-local date/time on all transcript messages and reasoning blocks; run full regression and production smoke.
  - Verify: `cd app && npm run lint && npm test && npm run build`
