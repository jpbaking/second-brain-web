# Milestone 38: Processing Animation and Thinking Blocks

- [x] m38-01: Update `toTranscript` in `ChatScreen.tsx` to handle `status` events (which may contain thinking or tool-use updates) distinctly from standard text chunks.
- [x] m38-02: Add a "processing" indicator in `ChatScreen.tsx` when a session is live (not ended) but waiting for the next agent event.
- [x] m38-03: Add relevant CSS to `.chat-thread` or `index.css` (using standard tokens) to make "thinking blocks" and the "processing" animation look premium and modern.
