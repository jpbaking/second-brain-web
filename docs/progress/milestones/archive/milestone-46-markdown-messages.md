# Milestone 46: Markdown rendering for agent messages

- [x] Add `react-markdown` to the `web` workspace. Verify: `npm ls --workspace web react-markdown` succeeds.
- [x] Render `ChatScreen` agent messages with `ReactMarkdown`. Verify: run `npm test` and `npm run build` from `app/` to ensure no breaks.
