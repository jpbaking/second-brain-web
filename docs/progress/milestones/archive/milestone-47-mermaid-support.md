# Milestone 47: Mermaid Support

- [x] Add `mermaid` to the `web` workspace. Verify: `npm ls --workspace web mermaid` succeeds.
- [x] Render `mermaid` code blocks as diagrams in `ChatScreen` messages. Verify: `npm test` and `npm run build` pass, and a test message with a mermaid block displays correctly.
