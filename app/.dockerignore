# Keep the build context small and force a clean install/build in the image.
# The context is the app/ directory (see docker-compose.yaml), so repo docs
# and compose files are already outside it.
**/node_modules
**/dist
**/*.log
# Never ship local state or secrets into an image.
data
**/.env
**/.env.*
**/*.sqlite
**/*.sqlite-*
